Redux saga is a library used for doing side effects:

These include:

Asynchronous data fetching.

Redux saga can be thought of as an organized way of helping the application made with redux to communicate and stay in sync with external dependencies like the API.