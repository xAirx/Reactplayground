// We will build a basic ReactJs application and containerize the app both for development and production.
// We will use Nginx for serving production build of our ReactJs application.

// Nginx is open - source software and a web server for reverse proxying caching and load balancing.
// Nginx also provides HTTP server capabilities.And it is mainly designed for maximum performance and stability.
// Nginx provides functions like IMAP, POP3, and SMTP as a proxy server for email.
